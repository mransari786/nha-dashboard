import { HealthidPartnerBO } from './healthid-partner-bo';

describe('HealthidPartnerBO', () => {
  it('should create an instance', () => {
    expect(new HealthidPartnerBO()).toBeTruthy();
  });
});
