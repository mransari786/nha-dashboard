import { HealthidCreatedBO } from './healthid-created-bo';

describe('HealthidCreatedBO', () => {
  it('should create an instance', () => {
    expect(new HealthidCreatedBO()).toBeTruthy();
  });
});
